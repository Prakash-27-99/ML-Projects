# ML-Projects

This project combines multiple CSV files into a single CSV file using Python and popular machine learning (ML) packages. The purpose of this script is to streamline data preprocessing by consolidating datasets into a unified format.
